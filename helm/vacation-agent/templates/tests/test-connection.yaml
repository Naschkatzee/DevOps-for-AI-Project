apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vacation-agent.fullname" . }}-test-connection"
  labels:
    {{- include "vacation-agent.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "vacation-agent.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
